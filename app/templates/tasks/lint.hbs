<div class="lint">
  <div class="lint-inner">

    {{!-- PAGE HEADER --}}
    <div class="header">
      <div class="header-inner">
        <div class="header-icon-square">
          <i class="ri-shield-check-line header-icon"></i>
        </div>
        <h1 class="header-title">lint</h1>
      </div>
    </div>

    {{!-- HORIRZONTAL SPLIT VIEW | left - right --}}
    <div class="lint-split">
      <div class="lint-split-right">

        {{!-- TOOLBAR --}}
        <div class="lint-toolbar">
          {{!-- TOOLBAR LEFT - buttons --}}
          <div class="lint-actions">
            <button type="button" class="flex items-center space-x-1 px-8 py-1 bg-gray-800 text-gray-100 rounded">
              <i class="ri-play-mini-line"></i>
              <span class="text-sm">Start</span>
            </button>
            <button type="button" class="flex items-center space-x-1 px-4 py-1 bg-gray-200 text-gray-800 rounded border border-gray-300">
              <i class="ri-settings-3-line"></i>
            </button>
          </div>

          {{!-- TOOLBAR RIGHT - command --}}
          <div class="lint-command">
            <span >ember lint --proxy http://localhost:90</span>
          </div>
        </div>

        {{!-- TERMINAL --}}
        <Terminal @command={{this.command}} @task="lint" @callback={{this.socketCallback}} />
      </div>

      {{!-- TODO: lint OPTIONS --}}
      {{!-- <div class="lint-split-left bg-gray-800 p-6">
        <h2 class="text-lg text-gray-500">Options</h2>
        <label for="">
          acceptance
          <input type="text">
        </label>
        <label for="">
          unit
          <input type="text">
        </label>
      </div> --}}
    </div>
  </div>
</div>
